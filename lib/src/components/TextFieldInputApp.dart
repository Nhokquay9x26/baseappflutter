import 'package:flutter/material.dart';

class TextFieldInputApp extends TextFormField {
  TextFieldInputApp({Key key}) : super(key: key);

  Widget build(BuildContext context) {
    return TextFormField(
    );
  }
}
